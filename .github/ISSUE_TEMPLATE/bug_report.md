---
name: Bug report
about: Report a problem with rdsclient, awsenv, or ec2client
labels: bug
---

<!--
Reports missing any of the fields below are likely to be closed without
investigation. Remember to sanitize output: remove account IDs, endpoints,
ARNs, and anything else you don't want public.
-->

**Exact command run**

```
```

**Version / commit** (`git rev-parse HEAD` or release tag)

**OS and shell** (e.g. macOS 15 / zsh, Ubuntu 24.04 / dash)

**Docker version** (`docker --version`)

**AWS CLI source** (local install or awsenv wrappers)

**What happened** (sanitized output)

```
```

**What you expected**
